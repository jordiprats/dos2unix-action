# dos2unix-action

Options:

* pattern:
  - description: 'Files to convert to unix format'
  - required: false
  - default: '*.md'


```
steps:
- uses: jordiprats/dos2unix-action@v1.0
  with:
    pattern: '*.md'
```
